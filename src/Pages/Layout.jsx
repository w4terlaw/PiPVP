import React from 'react';

const Layout = ({ children }) => {
    return (
        <div>
            <h1>React Router Example</h1>
            {children}
        </div>
    );
};

export default Layout;
